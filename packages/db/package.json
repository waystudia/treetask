{
  "name": "@treetask/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "node scripts/validate-migrations.mjs && tsc -p tsconfig.json",
    "lint": "node scripts/validate-migrations.mjs",
    "typecheck": "node scripts/validate-migrations.mjs && tsc -p tsconfig.json --noEmit",
    "test": "supabase test db --local"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
